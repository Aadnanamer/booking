class User {

  final String NAME;
  final String ID;
  final   String STAT;
  final String TYPE;
   User(  {required this.ID,required this.NAME,required this.TYPE,required this.STAT});
 // User(this.STAT, this.TYPE,   this.ID,  this.NAME, {required NAME});
}
